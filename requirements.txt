# This project uses only Python's built-in modules (csv, string, os).
# No external packages are required.
# Nothing need to install
# If you extend this project using NLTK for tokenization or stopword removal,
# add the following line and run: pip install -r requirements.txt
#
# nltk
